// Theme de l'App contenant les styles utilisés dans l'application
import 'package:flutter/material.dart';

const TextStyle title = TextStyle(
  fontFamily: 'Cera',
  fontSize: 25,
);

const TextStyle description = TextStyle(
  fontFamily: 'Cera',
  fontSize: 20,
);

// Color
const TextStyle blackTxt = TextStyle(color: Colors.black);
const TextStyle date = TextStyle(
  fontSize: 15,
  color: Colors.blue,
);
const Color amberCustom = Color.fromRGBO(255, 193, 7, 1);

// MediaQuery value, utilisation de variable représentant sur 1, la taille de la largeur de l'écran ou la hauteur

double heightMediumSpace = 0.1;
double widthBtnProfile = 0.4;
